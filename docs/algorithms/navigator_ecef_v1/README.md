# Navigator ECEF v1 Algorithm Specification

This folder contains the focused LaTeX algorithm contract for the first real
NavKit INS/GNSS navigator.

Build from this directory with:

```bash
latexmk -pdf main.tex
```

The document is intentionally narrower than `docs/navigation_reference`: it
defines the first implementation target rather than the long-term fully generic
navigation reference.
