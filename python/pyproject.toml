[project]
name = "navkit-analysis"
version = "0.1.0"
description = "Python plotting and analysis helpers for NavKit logs"
requires-python = ">=3.10"
dependencies = ["h5py", "numpy", "pandas", "matplotlib", "plotly", "scipy"]
